/// Represents a user of the Terrafarm app
/// Note that "Farmer" is used rather than "User", in order to distinguish between our model and the Firebase User class
class Farmer {

  final String uid;

  Farmer({ required this.uid });

}